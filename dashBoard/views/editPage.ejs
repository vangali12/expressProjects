<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<h1>Edit your Furry Friend!</h1>
	<form action='/otters/<%= otter[0]._id %>' method='post'>
		<p>Name: <input type="text" name="name" value="<%= otter[0].name %>"></p>
		<p>Age: <input type="text" name="age" value="<%= otter[0].age %>"></p>
		<p>Pebble Shape: <input type="text" name="pebbleShape" value="<%= otter[0].pebbleShape %>"></p>
		<input type="submit" name="submit">
	</form>
	<% if(typeof(errors) != 'undefined' ) { %>
		<% for (var x in errors) { %>
    	<h3><%= errors[x].message %></h3>
    	<% } %>
    <% } %>
</body>
</html>